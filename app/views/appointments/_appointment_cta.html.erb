<div class="max-w-md my-10 m-auto p-12 lg:p-16 bg-white rounded border border-grey-light text-center shadow">
  <h1 class="text-indigo-darker text-2xl mb-6">It looks like you do not have any appointment at this moment.</h1>
  <% if current_user.role == "patient" %>
    <%= link_to 'New Appointment', new_appointment_path, :class => 'btn btn-primary' %>
  <% end %>
</div>